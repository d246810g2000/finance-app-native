# Design system notes (M3 pass)

- **Primary seed**: `#2563EB` (finance trust blue). Not gold/purple.
- **Dynamic (Android 12+)**: surface, surfaceContainer, primary, onSurface, outline roles via `PlatformColor` system accent/neutral.
- **Fixed**: green / red (gain/loss), asset-class chart hues, gradients (Reanimated-safe hex).
- **Legacy aliases**: `bg→surface`, `card→surfaceContainer`, `accent→primary`, `textPrimary→onSurface`.
